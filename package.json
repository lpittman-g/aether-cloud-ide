{
  "name": "aether-cloud-ide",
  "version": "1.0.0",
  "private": true,
  "description": "Replit-style cloud IDE with Monaco, Socket.io, and sandboxed execution",
  "scripts": {
    "dev": "npm run dev --prefix backend & npm run dev --prefix frontend",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "typecheck": "npm run typecheck --prefix backend && npm run build --prefix frontend"
  },
  "dependencies": {
    "puppeteer-core": "^25.5.0"
  }
}
