# AGENTS.md

Read and follow **[docs/AGENT_PROTOCOL.md](./docs/AGENT_PROTOCOL.md)**.

Summary:

1. Commit code to GitHub on every change.  
2. Update `docs/` (especially `PROJECT_STATUS.md` and a session note) with non-secret context.  
3. Never commit credentials — see `docs/SECRETS.md`.
