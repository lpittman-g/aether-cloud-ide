import { StackPage } from "@/components/StackPage";

export const metadata = {
  title: "Stack — Aether",
  description:
    "How Aether is built: TypeScript IDE, sandbox runtimes, and Replit-inspired engine vs. user languages.",
};

export default function StackRoute() {
  return <StackPage />;
}
